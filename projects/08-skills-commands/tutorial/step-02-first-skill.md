# Step 2: 첫 번째 스킬 만들기 ⌨️

> 드디어 나만의 매크로를 만들 시간이에요! 🎉

---

## 🎯 이번 스텝의 목표

`/greet` 스킬을 직접 만들고 실행해보기

---

## 🎮 비유: 매크로 등록하기

게임에서 매크로를 만들려면:
1. 매크로 설정 창 열기 → `.claude/commands/` 폴더 만들기
2. 매크로 이름 정하기 → 파일 이름 정하기 (`greet.md`)
3. 매크로 동작 설정하기 → 마크다운 내용 작성하기
4. 매크로 실행! → `/greet 이름` 실행!

---

## ⌨️ 따라하기

### 1️⃣ 폴더 만들기

터미널에서 프로젝트 폴더로 이동한 후:

```bash
mkdir -p .claude/commands
```

> 💡 `mkdir -p`는 중간 폴더도 한번에 만들어줘요.
> `.claude` 폴더와 그 안의 `commands` 폴더가 한번에 생성!

### 폴더 구조 확인

```
내-프로젝트/
├── .claude/          ← 새로 생긴 폴더
│   └── commands/     ← 스킬 파일이 들어갈 곳
├── src/
└── package.json
```

---

### 2️⃣ 스킬 파일 작성하기

`.claude/commands/greet.md` 파일을 만들고 아래 내용을 적어요:

```markdown
사용자에게 친근하게 인사해주세요.

인사 대상: $ARGUMENTS

요구사항:
- 반말로 친근하게
- 이모지 포함
- 오늘의 격언 하나 추가
```

> 💡 **핵심**: `$ARGUMENTS` 자리에 사용자가 입력한 값이 들어가요!

---

### 3️⃣ 실행 테스트!

클로드 코드에서:

```
/greet 철수
```

**예상 결과**:

```
철수야 안녕! 👋

오늘도 좋은 하루 보내고 있어?
화이팅이다! 💪

🌟 오늘의 격언: "천 리 길도 한 걸음부터" — 노자
```

---

### 4️⃣ 다른 이름으로도 테스트

```
/greet 영희
```

```
/greet 우리 팀
```

> 💡 `$ARGUMENTS`에 뭘 넣든 동작해요! 이름, 팀명, 문장 다 OK!

---

## 🔍 동작 원리 복습

```
사용자 입력: /greet 철수
            ↓
    ┌─── 커맨드명: greet ────────────┐
    │   .claude/commands/greet.md     │
    │   파일을 찾아서 읽는다            │
    └─────────────────────────────────┘
            ↓
    ┌─── $ARGUMENTS 치환 ────────────┐
    │   $ARGUMENTS → "철수"           │
    │   "인사 대상: 철수"              │
    └─────────────────────────────────┘
            ↓
    ┌─── 클로드 AI 실행 ────────────┐
    │   완성된 프롬프트를 읽고          │
    │   응답 생성                      │
    └─────────────────────────────────┘
            ↓
    결과: "철수야 안녕! 👋..."
```

---

## 🛠️ 스킬 수정해보기

스킬을 더 재미있게 바꿔볼까요?

### 버전 2: 격식체로 변경

```markdown
사용자에게 격식체로 인사해주세요.

인사 대상: $ARGUMENTS

요구사항:
- 존댓말로 정중하게
- 이모지 포함
- 오늘의 날씨에 맞는 인사말 추가
- 명함을 건네는 느낌으로
```

### 버전 3: 랩 스타일

```markdown
사용자에게 랩으로 인사해주세요.

인사 대상: $ARGUMENTS

요구사항:
- 힙합 스타일 라임
- 4마디 이상
- 이모지 가득
- 마지막에 "마이크 드롭 🎤💥"으로 끝내기
```

> 💡 **재미 포인트**: 스킬 파일은 마크다운이니까, 내용을 바꾸면 동작도 바뀌어요!
> 수정 → 저장 → 바로 실행해보세요.

---

## ⚠️ 자주 하는 실수

| 실수 | 해결 |
|------|------|
| 폴더를 `claude/commands/`로 만듦 | `.claude/commands/`로! (점 필수!) |
| 파일을 `.txt`로 저장 | `.md`로 저장해야 해요 |
| `$arguments`로 적음 | `$ARGUMENTS` (대문자!) |
| 프로젝트 밖에서 실행 | 프로젝트 루트에서 클로드 코드 실행 |

---

## ✅ 체크리스트

- [ ] `.claude/commands/` 폴더 생성 완료
- [ ] `greet.md` 파일 작성 완료
- [ ] `/greet 이름`으로 실행 성공
- [ ] 다른 이름으로도 테스트 완료
- [ ] (보너스) 스킬 내용을 수정해서 다시 실행

---

## 💡 핵심 정리

| 번호 | 핵심 |
|------|------|
| 1 | `.claude/commands/` 폴더에 `.md` 파일을 만들면 스킬 완성! |
| 2 | 파일 이름 = 커맨드 이름 (`greet.md` → `/greet`) |
| 3 | `$ARGUMENTS`에 사용자 입력이 들어감 |
| 4 | 파일을 수정하면 스킬 동작도 즉시 변경됨 |
| 5 | 복잡한 설정 없음! 그냥 마크다운 쓰면 끝 ✨ |

> ⌨️ **다음 스텝**: 진짜 실무에서 쓸 수 있는 고급 스킬을 만들어볼까요?
> → [Step 3: 실용 스킬 활용하기](step-03-advanced.md)
