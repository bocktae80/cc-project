# 인증 관리 — claude auth 실습 ⭐

> 집에 들어가려면 열쇠가 필요하듯, 클로드를 쓰려면 **인증(로그인)**이 필요해요!

`claude auth`는 클로드 코드의 **열쇠 관리 명령어**입니다.
로그인하고, 상태를 확인하고, 로그아웃하는 3가지 기능이 있어요.

---

## 리모컨 비유

```
TV 리모컨의 "계정" 메뉴
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  📺 로그인     →  claude auth login    (열쇠로 문 열기)
  📺 내 정보    →  claude auth status   (열쇠 상태 확인)
  📺 로그아웃   →  claude auth logout   (열쇠 반납)
```

---

## 3가지 서브커맨드

| 명령어 | 하는 일 | 비유 |
|--------|---------|------|
| `claude auth login` | Anthropic 계정으로 로그인 | 열쇠로 문 열기 |
| `claude auth status` | 현재 로그인 상태 확인 | 열쇠가 유효한지 점검 |
| `claude auth logout` | 로그아웃 | 열쇠 반납 |

---

## 따라하기

### 1단계: 현재 상태 확인하기

터미널을 열고 다음을 입력하세요:

```bash
claude auth status
```

로그인이 되어 있다면 이런 결과가 나옵니다:

```
Authenticated as: kent@example.com
Account type: Pro
Session expires: 2026-03-22T10:00:00Z
```

로그인이 안 되어 있다면:

```
Not authenticated. Run 'claude auth login' to sign in.
```

### 2단계: 로그인하기

아직 로그인이 안 되어 있다면:

```bash
claude auth login
```

실행하면 브라우저가 열리면서 Anthropic 계정 로그인 페이지가 표시됩니다.

```
Opening browser for authentication...
Waiting for login...

✓ Successfully authenticated as kent@example.com
```

> **참고:** 브라우저가 자동으로 안 열리면, 터미널에 표시된 URL을 복사해서 직접 브라우저에 붙여넣으세요.

### 3단계: 다시 상태 확인

로그인 후 다시 확인해보세요:

```bash
claude auth status
```

```
Authenticated as: kent@example.com
Account type: Pro
Session expires: 2026-03-22T10:00:00Z
```

이제 정상적으로 인증 정보가 표시됩니다!

### 4단계: 로그아웃

실습을 위해 로그아웃도 해봅시다:

```bash
claude auth logout
```

```
✓ Successfully logged out.
```

> **주의:** 로그아웃하면 다시 `claude auth login`을 해야 클로드를 사용할 수 있어요!

---

## 실전 시나리오

### 시나리오 1: "클로드가 갑자기 안 돼요!"

```bash
# 1. 먼저 상태를 확인해본다
claude auth status

# 결과: "Not authenticated" 또는 "Session expired"
# → 세션이 만료되었구나!

# 2. 다시 로그인
claude auth login

# → 문제 해결!
```

### 시나리오 2: "다른 계정으로 바꾸고 싶어요"

```bash
# 1. 현재 계정 로그아웃
claude auth logout

# 2. 새 계정으로 로그인
claude auth login

# → 브라우저에서 다른 계정 선택
```

### 시나리오 3: "여러 프로젝트에서 다른 계정을 쓰고 싶어요"

각 프로젝트 폴더에서 로그인 상태가 공유됩니다.
계정을 바꾸려면 로그아웃 후 다시 로그인하세요.

```bash
# 프로젝트 A에서 작업 완료 후
claude auth logout

# 프로젝트 B로 이동
cd ~/project-b

# 다른 계정으로 로그인
claude auth login
```

---

## 자주 묻는 질문

| 질문 | 답변 |
|------|------|
| 매번 로그인해야 하나요? | 아니요! 한 번 로그인하면 세션이 유지됩니다 |
| 세션은 언제 만료되나요? | 일정 기간 후 자동 만료됩니다. `auth status`로 확인하세요 |
| 인터넷 없이도 되나요? | 최초 로그인은 인터넷이 필요합니다 |

---

## 핵심 정리

```
claude auth 한 줄 요약
━━━━━━━━━━━━━━━━━━━━━━━━━

  login  = 열쇠 받기 (로그인)
  status = 열쇠 확인 (상태 점검)
  logout = 열쇠 반납 (로그아웃)

  안 되면? → status 먼저 확인!
```

> **다음 단계:** [에이전트 & 이름](../agent-list/)에서 `claude agents`와 `/rename`을 배워보세요!
