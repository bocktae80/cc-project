# 도전 과제: 주사위 MCP 서버 만들기 🎲

> 지금까지 배운 걸 활용해서 나만의 MCP 서버를 만들어봅시다!

---

## 미션 🎯

**주사위를 굴려주는 MCP 서버**를 만들어보세요!

### 완성하면 이렇게 동작해요:

```
사용자: "주사위 굴려줘"
클로드 → MCP 서버: tools/call: roll_dice
MCP 서버 → 클로드: { result: 4 }
클로드: "🎲 주사위 결과: 4가 나왔어요!"
```

---

## 요구사항 📋

### 필수 기능 (⭐)

1. **`roll_dice` 도구**: 1~6 사이의 랜덤 숫자를 반환
2. MCP SDK 사용 (`@modelcontextprotocol/sdk`)
3. `package.json`에 `"type": "module"` 설정

### 보너스 기능 (⭐⭐)

4. **면 수 선택**: 주사위 면 수를 파라미터로 받기 (기본값: 6)
   - 예: "20면 주사위 굴려줘" → 1~20 사이 랜덤

### 고급 기능 (⭐⭐⭐)

5. **여러 개 굴리기**: 주사위 개수를 파라미터로 받기
   - 예: "6면 주사위 3개 굴려줘" → [3, 1, 5]
6. **합계 표시**: 여러 주사위의 합계도 알려주기

---

## 힌트 💡

### 힌트 1: 랜덤 숫자 만들기

```javascript
// 1~6 사이 랜덤 숫자
const result = Math.floor(Math.random() * 6) + 1;
```

### 힌트 2: 파라미터 받기

```javascript
import { z } from "zod";

server.tool(
  "roll_dice",
  "주사위를 굴립니다",
  {
    sides: z.number().default(6).describe("주사위 면 수"),
  },
  async ({ sides }) => {
    const result = Math.floor(Math.random() * sides) + 1;
    // ...
  }
);
```

### 힌트 3: 여러 개 굴리기

```javascript
const results = [];
for (let i = 0; i < count; i++) {
  results.push(Math.floor(Math.random() * sides) + 1);
}
const sum = results.reduce((a, b) => a + b, 0);
```

---

## 파일 구조

이 폴더에 아래 파일들을 만들어보세요:

```
exercise/
├── my-dice-server.js    ← 여기에 서버 코드 작성!
├── package.json         ← 패키지 설정
├── .claude/
│   └── settings.json    ← MCP 서버 연결 설정 (이미 있음)
└── README.md            ← 이 파일
```

---

## 테스트 방법 🧪

### 1. 패키지 설치

```bash
npm install
```

### 2. 서버 테스트

```bash
node my-dice-server.js
# 대기 상태가 되면 성공! (Ctrl+C로 종료)
```

### 3. Claude Code에 연결

```bash
claude mcp add dice -- node $(pwd)/my-dice-server.js
```

### 4. 사용해보기

```
"주사위 굴려줘"
"20면 주사위 굴려줘"
"6면 주사위 5개 굴려줘"
```

---

## 정답 확인 체크리스트 ✅

- [ ] `roll_dice` 도구가 동작하는가?
- [ ] 1~6 사이 숫자가 나오는가?
- [ ] Claude Code에서 "주사위 굴려줘"에 반응하는가?
- [ ] (보너스) 면 수를 바꿀 수 있는가?
- [ ] (고급) 여러 개를 동시에 굴릴 수 있는가?

---

> 💡 **막히면?** `examples/custom-server/my-time-server.js`를 참고하세요!
> 구조는 거의 똑같고, 시간 계산 부분만 주사위 로직으로 바꾸면 됩니다.
