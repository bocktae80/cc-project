# Glob 패턴 마스터하기 🗂️

> 📋 도서관 카탈로그처럼 — 파일 이름과 경로로 찾기!

Glob은 **파일 이름 패턴 매칭** 도구입니다. 와일드카드(특수 문자)를 사용해서 여러 파일을 한 번에 찾아요.

마치 도서관에서 "3층 과학 코너에 있는 책 전부 보여줘"라고 하는 것과 같습니다!

---

## 🃏 와일드카드 4종 카드

| 와일드카드 | 이름 | 의미 | 비유 |
|-----------|------|------|------|
| `*` | 별표 | 한 폴더 안에서 아무 글자 | "이 방에 있는 것 다 줘" |
| `**` | 더블 별표 | 모든 폴더를 뚫고 검색 | "이 건물 전체를 뒤져줘" |
| `?` | 물음표 | 딱 한 글자만 | "_ 한 칸 빈칸" |
| `{}` | 중괄호 | 여러 선택지 중 하나 | "이것 또는 저것" |

---

## ⭐ 와일드카드 하나씩 배우기

### 1. `*` (별표) — "이 방에 있는 것 다 줘"

`*`는 **현재 폴더**에서 아무 문자열과 매칭됩니다. 단, 폴더 경계(`/`)를 넘지 않아요.

```
패턴: *.js
의미: 현재 폴더에서 .js로 끝나는 모든 파일
```

#### 🎮 playground에서 실습

```
Glob: pattern="*.json" path="playground/src/"
```

**결과:**
```
playground/src/config.json    ← 1개만 매칭!
```

```
Glob: pattern="*.js" path="playground/src/"
```

**결과:**
```
playground/src/app.js
playground/src/utils.js       ← 2개 매칭 (components/ 안의 파일은 안 나옴!)
```

> 💡 `*`는 현재 폴더만 봐요. 하위 폴더는 안 봅니다!

---

### 2. `**` (더블 별표) — "건물 전체를 뒤져줘"

`**`는 **모든 하위 폴더**를 포함해서 검색합니다. 가장 많이 쓰는 패턴이에요!

```
패턴: **/*.js
의미: 모든 폴더에서 .js로 끝나는 파일 전부
```

#### 🎮 playground에서 실습

```
Glob: pattern="**/*.js" path="playground/"
```

**결과:**
```
playground/src/app.js
playground/src/utils.js
playground/src/components/Header.js
playground/src/components/Footer.js
playground/src/components/TodoItem.js
playground/tests/app.test.js
playground/tests/utils.test.js
```

7개 전부 찾았습니다! 📋

> 💡 `**`는 폴더를 뚫고 들어갑니다. 진짜 많이 써요!

---

### 3. `?` (물음표) — "빈칸 하나"

`?`는 **정확히 한 글자**와 매칭됩니다.

```
패턴: ?.js
의미: 한 글자 이름의 .js 파일
```

솔직히 `?`는 자주 쓰이지 않지만, 알아두면 가끔 유용해요.

#### 🎮 예시

```
Glob: pattern="app.????" path="playground/tests/"
```

이렇게 하면 `app.test` (4글자 확장자)... 는 사실 `app.test.js`라서 안 맞겠네요 😅

> 💡 `?`는 실전에서는 거의 안 씁니다. `*`와 `**`만 잘 알면 OK!

---

### 4. `{}` (중괄호) — "이것 또는 저것"

`{}`는 **여러 선택지** 중 하나와 매칭됩니다.

```
패턴: **/*.{js,json}
의미: 모든 폴더에서 .js 또는 .json 파일
```

#### 🎮 playground에서 실습

```
Glob: pattern="**/*.{js,json}" path="playground/"
```

**결과:**
```
playground/src/app.js
playground/src/utils.js
playground/src/config.json         ← .json도 포함!
playground/src/components/Header.js
playground/src/components/Footer.js
playground/src/components/TodoItem.js
playground/tests/app.test.js
playground/tests/utils.test.js
playground/package.json            ← 이것도!
```

```
Glob: pattern="**/*.{css,md}" path="playground/"
```

**결과:**
```
playground/styles/main.css
playground/styles/components.css
playground/docs/guide.md
playground/docs/api.md
```

> 💡 `{}`는 여러 확장자를 한 번에 찾을 때 완벽!

---

## 📋 자주 쓰는 패턴 모음표

| 하고 싶은 것 | Glob 패턴 | 설명 |
|-------------|----------|------|
| 모든 JS 파일 | `**/*.js` | 전체 검색 기본 |
| 모든 테스트 파일 | `**/*.test.js` | 테스트만 골라내기 |
| 특정 폴더의 파일 | `src/*.js` | src 바로 아래만 |
| CSS + JS 한번에 | `**/*.{css,js}` | 중괄호 활용 |
| 모든 설정 파일 | `**/*.{json,yaml,yml}` | 설정 파일 총집합 |
| 문서 파일 | `**/*.md` | 마크다운만 |
| 컴포넌트 폴더 | `**/components/*` | 컴포넌트만 |
| package.json 찾기 | `**/package.json` | 어디든 |
| 숨김 파일 제외하고 전부 | `**/*` | 모든 파일 |

---

## 🧪 연습 문제

playground 프로젝트를 대상으로 다음을 찾아보세요:

### Q1. 모든 CSS 파일을 찾아보세요
<details>
<summary>💡 정답 보기</summary>

```
Glob: pattern="**/*.css" path="playground/"
```
결과: `main.css`, `components.css` (2개)
</details>

### Q2. tests 폴더의 파일만 찾아보세요
<details>
<summary>💡 정답 보기</summary>

```
Glob: pattern="tests/*" path="playground/"
```
결과: `app.test.js`, `utils.test.js` (2개)
</details>

### Q3. .js와 .css 파일을 한 번에 찾아보세요
<details>
<summary>💡 정답 보기</summary>

```
Glob: pattern="**/*.{js,css}" path="playground/"
```
결과: JS 7개 + CSS 2개 = 9개
</details>

### Q4. components 폴더의 파일만 찾아보세요
<details>
<summary>💡 정답 보기</summary>

```
Glob: pattern="**/components/*" path="playground/"
```
결과: `Header.js`, `Footer.js`, `TodoItem.js` (3개)
</details>

---

## 💡 핵심 정리

```
┌─────────────────────────────────────────┐
│ Glob 와일드카드 4종 카드                │
│                                         │
│  *    한 폴더에서 아무거나              │
│  **   모든 폴더를 뚫고                  │
│  ?    딱 한 글자                        │
│  {}   이것 또는 저것                    │
│                                         │
│ 🏆 가장 많이 쓰는 패턴: **/*.확장자    │
└─────────────────────────────────────────┘
```

> ✅ 다음: [Grep 검색 배우기](../grep-search/README.md)
